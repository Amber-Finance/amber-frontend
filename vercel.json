{
  "buildCommand": "cd apps/app && pnpm build",
  "outputDirectory": "apps/app/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "build": {
    "env": {
      "NODE_OPTIONS": "--max-old-space-size=4096"
    }
  }
}
